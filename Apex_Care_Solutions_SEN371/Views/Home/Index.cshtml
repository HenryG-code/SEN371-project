﻿
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ApexCare Solutions</title>
    <style>
        /* Basic reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body Styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
        }

        /* Header and Navigation Bar */
        header {
            background-color: #fff;
            padding: 20px;
            border-bottom: 1px solid #ddd;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

            nav ul {
                display: flex;
                list-style: none;
            }

                nav ul li {
                    margin: 0 15px;
                }

                    nav ul li a {
                        text-decoration: none;
                        color: #333;
                        font-weight: bold;
                    }

            nav .auth-buttons {
                display: flex;
                gap: 10px;
                align-items: center;
            }

        .auth-buttons button {
            background-color: #333;
            color: #fff;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
        }

            .auth-buttons button:hover {
                background-color: #555;
            }

        /* Main Section */
        main {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 50px 20px;
        }

        h1 {
            font-size: 36px;
            color: #333;
        }

        h2 {
            font-size: 18px;
            color: #888;
            margin-bottom: 40px;
        }

        .main-buttons {
            display: flex;
            gap: 20px;
            margin-bottom: 40px;
        }

            .main-buttons button {
                padding: 15px 25px;
                font-size: 16px;
                border: 1px solid #ccc;
                background-color: #fff;
                cursor: pointer;
                border-radius: 5px;
            }

                .main-buttons button:hover {
                    background-color: #f0f0f0;
                }

        /* Footer */
        footer {
            background-color: #f7f7f7;
            padding: 30px;
            border-top: 1px solid #ddd;
            text-align: center;
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 50px;
        }

            .footer-content ul {
                list-style: none;
                display: flex;
                gap: 20px;
            }

                .footer-content ul li {
                    display: inline-block;
                }

                    .footer-content ul li a {
                        text-decoration: none;
                        color: #333;
                        font-weight: bold;
                    }

            .footer-content .logo {
                font-size: 20px;
                font-weight: bold;
            }

    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <header>
        <nav>
            <ul>
            </ul>
            <!--Login button and Register button-->
            <div class="auth-buttons">
                <button onclick="location.href='@Url.Action("Login", "Home")'" class="btn btn-dark">Sign In</button>
                <button onclick="location.href='@Url.Action("Register", "Home")'" class="btn btn-outline-dark">Register</button>
            </div>
        </nav>
    </header>

    <!-- Main Section -->
    <main>
        <h1>ApexCare Solutions</h1>
        <h2>We strive to provide you with the best service</h2>

        <!--ServiceDesk button and Privacy(Contract Management) button-->
        <div class="auth-buttons">
            <button onclick="location.href='@Url.Action("ServiceDesk", "Home")'" class="btn btn-dark">Log a fault</button>
            <button onclick="location.href='@Url.Action("Privacy", "Home")'" class="btn btn-outline-dark">View Contracts</button>
        </div>

    </main>
</body>
</html>

